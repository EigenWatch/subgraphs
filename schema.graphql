# ========================================
# EIGENWATCH EVENT-FOCUSED SUBGRAPH SCHEMA
# Focus: Pure event capture with minimal entity state
# ========================================

# ========================================
# BASE EVENT INTERFACE
# ========================================

interface BaseEvent {
  id: ID!
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!
}

# ========================================
# MINIMAL LOOKUP ENTITIES
# (Only for GraphQL relationships - no complex state)
# ========================================

type Operator @entity(immutable: false) {
  id: ID! # operator address
  address: Bytes!

  # Relationships to events only
  registrationEvents: [OperatorRegistered!]! @derivedFrom(field: "operator")
  shareEvents: [OperatorShareEvent!]! @derivedFrom(field: "operator")
  slashingEvents: [OperatorSlashed!]! @derivedFrom(field: "operator")
  avsRegistrationEvents: [OperatorAVSRegistrationStatusUpdated!]!
    @derivedFrom(field: "operator")
  operatorSetJoinEvents: [OperatorAddedToOperatorSet!]!
    @derivedFrom(field: "operator")
  operatorSetLeaveEvents: [OperatorRemovedFromOperatorSet!]!
    @derivedFrom(field: "operator")
  allocationEvents: [AllocationEvent!]! @derivedFrom(field: "operator")
  metadataUpdateEvents: [OperatorMetadataUpdate!]!
    @derivedFrom(field: "operator")
  delegationApproverUpdates: [DelegationApproverUpdated!]!
    @derivedFrom(field: "operator")
}

type Staker @entity(immutable: false) {
  id: ID! # staker address
  address: Bytes!

  # Relationships to events only
  delegationEvents: [StakerDelegationEvent!]! @derivedFrom(field: "staker")
  shareEvents: [OperatorShareEvent!]! @derivedFrom(field: "staker")
  depositEvents: [Deposit!]! @derivedFrom(field: "staker")
  withdrawalEvents: [WithdrawalEvent!]! @derivedFrom(field: "staker")
  podDeploymentEvents: [PodDeployed!]! @derivedFrom(field: "owner")
  beaconChainDepositEvents: [BeaconChainDeposit!]!
    @derivedFrom(field: "podOwner")
  beaconChainWithdrawalEvents: [BeaconChainWithdrawal!]!
    @derivedFrom(field: "podOwner")
  podSharesUpdateEvents: [PodSharesUpdate!]! @derivedFrom(field: "podOwner")
  beaconChainSlashingEvents: [BeaconChainSlashingEvent!]!
    @derivedFrom(field: "staker")
  forceUndelegationEvents: [StakerForceUndelegated!]!
    @derivedFrom(field: "staker")
  depositScalingEvents: [DepositScalingFactorUpdated!]!
    @derivedFrom(field: "staker")
}

type AVS @entity(immutable: false) {
  id: ID! # avs address
  address: Bytes!

  # Relationships to events only
  operatorRegistrationEvents: [OperatorAVSRegistrationStatusUpdated!]!
    @derivedFrom(field: "avs")
  rewardsSubmissionEvents: [RewardsSubmission!]! @derivedFrom(field: "avs")
  operatorDirectedRewardsEvents: [OperatorDirectedAVSRewardsSubmission!]!
    @derivedFrom(field: "avs")
  metadataUpdateEvents: [AVSMetadataUpdate!]! @derivedFrom(field: "avs")
  operatorSetCreationEvents: [OperatorSetCreated!]! @derivedFrom(field: "avs")
  registrarSetEvents: [AVSRegistrarSet!]! @derivedFrom(field: "avs")
}

type Strategy @entity(immutable: false) {
  id: ID! # strategy contract address
  address: Bytes!

  # Relationships to events only
  depositEvents: [Deposit!]! @derivedFrom(field: "strategy")
  shareEvents: [OperatorShareEvent!]! @derivedFrom(field: "strategy")
  allocationEvents: [AllocationEvent!]! @derivedFrom(field: "strategy")
  whitelistEvents: [StrategyWhitelistEvent!]! @derivedFrom(field: "strategy")
  strategyOperatorSetEvents: [StrategyOperatorSetEvent!]!
    @derivedFrom(field: "strategy")
  # Note: slashingEvents removed - strategies field is [String!] array, not Strategy entities
}

type OperatorSet @entity(immutable: false) {
  id: ID! # avs-operatorSetId composite
  avs: AVS!
  operatorSetId: BigInt!

  # Relationships to events only
  creationEvent: [OperatorSetCreated!]! @derivedFrom(field: "operatorSet")
  memberJoinEvents: [OperatorAddedToOperatorSet!]!
    @derivedFrom(field: "operatorSet")
  memberLeaveEvents: [OperatorRemovedFromOperatorSet!]!
    @derivedFrom(field: "operatorSet")
  allocationEvents: [AllocationEvent!]! @derivedFrom(field: "operatorSet")
  slashingEvents: [OperatorSlashed!]! @derivedFrom(field: "operatorSet")
  strategyEvents: [StrategyOperatorSetEvent!]!
    @derivedFrom(field: "operatorSet")
  redistributionEvents: [RedistributionAddressSet!]!
    @derivedFrom(field: "operatorSet")
  operatorDirectedRewardsEvents: [OperatorDirectedOperatorSetRewardsSubmission!]!
    @derivedFrom(field: "operatorSet")
}

type EigenPod @entity(immutable: false) {
  id: ID! # pod address
  address: Bytes!
  owner: Staker!

  # Relationships to events only
  deploymentEvent: [PodDeployed!]! @derivedFrom(field: "pod")
  beaconChainDepositEvents: [BeaconChainDeposit!]! @derivedFrom(field: "pod")
  shareUpdateEvents: [PodSharesUpdate!]! @derivedFrom(field: "pod")
  beaconChainWithdrawalEvents: [BeaconChainWithdrawal!]!
    @derivedFrom(field: "pod")
}

# ========================================
# RAW EVENT ENTITIES (Direct from blockchain)
# All events are immutable and contain complete event data
# ========================================

# DELEGATION MANAGER EVENTS

type OperatorRegistered implements BaseEvent @entity(immutable: true) {
  id: ID!
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operator: Operator!
  delegationApprover: Bytes!
}

type DelegationApproverUpdated implements BaseEvent @entity(immutable: true) {
  id: ID!
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operator: Operator!
  newDelegationApprover: Bytes!
}

type OperatorMetadataUpdate implements BaseEvent @entity(immutable: true) {
  id: ID!
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operator: Operator!
  metadataURI: String!
}

type OperatorShareEvent implements BaseEvent @entity(immutable: true) {
  id: ID!
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operator: Operator!
  staker: Staker!
  strategy: Strategy!
  shares: BigInt!
  eventType: ShareEventType!
}

type StakerDelegationEvent implements BaseEvent @entity(immutable: true) {
  id: ID!
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  staker: Staker!
  operator: Operator!
  delegationType: DelegationType!
}

type StakerForceUndelegated implements BaseEvent @entity(immutable: true) {
  id: ID!
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  staker: Staker!
  operator: Operator!
}

type DepositScalingFactorUpdated implements BaseEvent @entity(immutable: true) {
  id: ID!
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  staker: Staker!
  strategy: Strategy!
  newDepositScalingFactor: BigInt!
}

type WithdrawalEvent implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  withdrawalRoot: Bytes!
  staker: Staker
  delegatedTo: Operator
  withdrawer: Bytes!
  nonce: BigInt!
  startBlock: BigInt
  strategies: [String!]!
  shares: [BigInt!]!
  eventType: WithdrawalEventType!

  "Indicates if this record has complete data (false for COMPLETED events from SlashingWithdrawalCompleted)"
  isComplete: Boolean!
}

type OperatorSharesSlashed implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operator: Operator!
  strategy: Strategy!
  totalSlashedShares: BigInt!
}

# ALLOCATION MANAGER EVENTS

type AllocationDelaySet implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operator: Operator!
  delay: BigInt! # uint32
  effectBlock: BigInt! # uint32
}

type AllocationEvent implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operator: Operator!
  operatorSet: OperatorSet!
  strategy: Strategy!
  magnitude: BigInt! # uint64
  effectBlock: BigInt! # uint32
}

type EncumberedMagnitudeUpdated implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operator: Operator!
  strategy: Strategy!
  encumberedMagnitude: BigInt! # uint64
}

type MaxMagnitudeUpdated implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operator: Operator!
  strategy: Strategy!
  maxMagnitude: BigInt! # uint64
}

type OperatorSlashed implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operator: Operator!
  operatorSet: OperatorSet!
  strategies: [String!]! # Strategy addresses
  wadSlashed: [BigInt!]!
  description: String!
}

type AVSRegistrarSet implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  avs: AVS!
  registrar: Bytes! # IAVSRegistrar address
}

type AVSMetadataUpdate implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  avs: AVS!
  metadataURI: String!
}

type OperatorSetCreated implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operatorSet: OperatorSet!
  avs: AVS!
  operatorSetId: BigInt!
}

type OperatorAddedToOperatorSet implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operator: Operator!
  operatorSet: OperatorSet!
}

type OperatorRemovedFromOperatorSet implements BaseEvent
  @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operator: Operator!
  operatorSet: OperatorSet!
}

type RedistributionAddressSet implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operatorSet: OperatorSet!
  redistributionRecipient: Bytes!
}

type StrategyOperatorSetEvent implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operatorSet: OperatorSet!
  strategy: Strategy!
  eventType: StrategyOperatorSetEventType!
}

# REWARDS COORDINATOR EVENTS

type RewardsSubmission implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  avs: AVS
  submitter: Bytes!
  submissionNonce: BigInt!
  rewardsSubmissionHash: Bytes!
  submissionType: RewardsSubmissionType!

  # Submission details (JSON encoded from struct)
  strategiesAndMultipliers: String! # JSON array
  token: Bytes!
  amount: BigInt!
  startTimestamp: BigInt!
  duration: BigInt!
}

type OperatorDirectedAVSRewardsSubmission implements BaseEvent
  @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  caller: Bytes!
  avs: AVS!
  operatorDirectedRewardsSubmissionHash: Bytes!
  submissionNonce: BigInt!

  # Submission details
  strategiesAndMultipliers: String! # JSON array
  token: Bytes!
  operatorRewards: String! # JSON array of operator rewards
  startTimestamp: BigInt!
  duration: BigInt!
  description: String!
}

type OperatorDirectedOperatorSetRewardsSubmission implements BaseEvent
  @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  caller: Bytes!
  operatorDirectedRewardsSubmissionHash: Bytes!
  operatorSet: OperatorSet!
  submissionNonce: BigInt!

  # Submission details
  strategiesAndMultipliers: String! # JSON array
  token: Bytes!
  operatorRewards: String! # JSON array of operator rewards
  startTimestamp: BigInt!
  duration: BigInt!
  description: String!
}

type RewardsUpdaterSet implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  oldRewardsUpdater: Bytes!
  newRewardsUpdater: Bytes!
}

type RewardsForAllSubmitterSet implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  rewardsForAllSubmitter: Bytes!
  oldValue: Boolean!
  newValue: Boolean!
}

type ActivationDelaySet implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  oldActivationDelay: BigInt! # uint32
  newActivationDelay: BigInt! # uint32
}

type DefaultOperatorSplitBipsSet implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  oldDefaultOperatorSplitBips: BigInt! # uint16
  newDefaultOperatorSplitBips: BigInt! # uint16
}

type OperatorAVSSplitBipsSet implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  caller: Bytes!
  operator: Operator!
  avs: AVS!
  activatedAt: BigInt! # uint32
  oldOperatorAVSSplitBips: BigInt! # uint16
  newOperatorAVSSplitBips: BigInt! # uint16
}

type OperatorPISplitBipsSet implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  caller: Bytes!
  operator: Operator!
  activatedAt: BigInt! # uint32
  oldOperatorPISplitBips: BigInt! # uint16
  newOperatorPISplitBips: BigInt! # uint16
}

type OperatorSetSplitBipsSet implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  caller: Bytes!
  operator: Operator!
  operatorSet: OperatorSet!
  activatedAt: BigInt! # uint32
  oldOperatorSetSplitBips: BigInt! # uint16
  newOperatorSetSplitBips: BigInt! # uint16
}

type ClaimerForSet implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  earner: Bytes!
  oldClaimer: Bytes!
  claimer: Bytes!
}

type DistributionRootSubmitted implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  rootIndex: BigInt! # uint32
  root: Bytes!
  rewardsCalculationEndTimestamp: BigInt! # uint32
  activatedAt: BigInt! # uint32
}

type DistributionRootDisabled implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  rootIndex: BigInt! # uint32
}

type RewardsClaimed implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  root: Bytes!
  earner: Bytes!
  claimer: Bytes!
  recipient: Bytes!
  token: Bytes!
  claimedAmount: BigInt!
}

# STRATEGY MANAGER EVENTS

type Deposit implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  staker: Staker!
  strategy: Strategy!
  shares: BigInt!
}

type StrategyWhitelisterChanged implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  previousAddress: Bytes!
  newAddress: Bytes!
}

type StrategyWhitelistEvent implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  strategy: Strategy!
  eventType: StrategyWhitelistEventType!
}

type BurnOrRedistributableSharesIncreased implements BaseEvent
  @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operatorSet: OperatorSet!
  slashId: BigInt!
  strategy: Strategy!
  shares: BigInt!
}

type BurnOrRedistributableSharesDecreased implements BaseEvent
  @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operatorSet: OperatorSet!
  slashId: BigInt!
  strategy: Strategy!
  shares: BigInt!
}

type BurnableSharesDecreased implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  strategy: Strategy!
  shares: BigInt!
}

# AVS DIRECTORY EVENTS

type OperatorAVSRegistrationStatusUpdated implements BaseEvent
  @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  operator: Operator!
  avs: AVS!
  status: AVSRegistrationStatus!
}

# EIGEN POD MANAGER EVENTS

type PodDeployed implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  pod: EigenPod!
  owner: Staker!
}

type BeaconChainDeposit implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  pod: EigenPod
  podOwner: Staker!
  amount: BigInt!
}

type PodSharesUpdate implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  pod: EigenPod
  podOwner: Staker!
  sharesDelta: BigInt! # int256
  newTotalShares: BigInt
  updateType: PodSharesUpdateType!
}

type BeaconChainWithdrawal implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  pod: EigenPod
  podOwner: Staker!
  shares: BigInt!
  nonce: BigInt!
  delegatedAddress: Bytes!
  withdrawer: Bytes!
  withdrawalRoot: Bytes!
}

type BeaconChainETHWithdrawalCompleted implements BaseEvent
  @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  podOwner: Staker!
  shares: BigInt!
  nonce: BigInt! # uint96
  delegatedAddress: Bytes!
  withdrawer: Bytes!
  withdrawalRoot: Bytes!
}

type BeaconChainSlashingEvent implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  staker: Staker!
  prevBeaconChainSlashingFactor: BigInt! # uint64
  newBeaconChainSlashingFactor: BigInt! # uint64
}

type BurnableETHSharesIncreased implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  shares: BigInt!
}

type PectraForkTimestampSet implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  newPectraForkTimestamp: BigInt! # uint64
}

type ProofTimestampSetterSet implements BaseEvent @entity(immutable: true) {
  id: ID! # txHash-logIndex
  transactionHash: Bytes!
  logIndex: BigInt!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  contractAddress: Bytes!

  # Event data
  newProofTimestampSetter: Bytes!
}

# ========================================
# ENUM TYPES
# ========================================

enum DelegationType {
  DELEGATED
  UNDELEGATED
  FORCE_UNDELEGATED
}

enum ShareEventType {
  INCREASED
  DECREASED
}

enum WithdrawalEventType {
  QUEUED
  COMPLETED
}

enum AVSRegistrationStatus {
  REGISTERED
  UNREGISTERED
}

enum StrategyOperatorSetEventType {
  ADDED
  REMOVED
}

enum RewardsSubmissionType {
  AVS_REWARDS
  REWARDS_FOR_ALL
  REWARDS_FOR_ALL_EARNERS
  OPERATOR_DIRECTED_AVS
  OPERATOR_DIRECTED_OPERATOR_SET
}

enum StrategyWhitelistEventType {
  ADDED
  REMOVED
}

enum PodSharesUpdateType {
  SHARES_UPDATED
  NEW_TOTAL_SHARES
}
